# Depth comparison benchmark parameters — 3-model edition
# Changed from v2: 3 mono scan topics (metric_large, metric_small, relative)
# Used by: depth_comparison_test.launch.py → depth_scan_benchmark_node
# DO NOT EDIT — create a new versioned file (e.g., depth_comparison_benchmark_v4.yaml)
depth_scan_benchmark:
  ros__parameters:
    stereo_scan_topic: "/scan_stereo"
    mono_scan_topic: "/scan_metric_large"
    mono_d2l_scan_topic: "/scan_metric_small"
    output_dir: "/home/sidd/wheelchair_nav/eval_output/depth_comparison"
    sync_slop: 0.5
    fp_threshold_m: 0.5
    summary_interval: 25
