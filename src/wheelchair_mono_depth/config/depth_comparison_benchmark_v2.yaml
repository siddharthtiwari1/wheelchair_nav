# Depth comparison benchmark parameters
# Changed from v1: wider sync_slop (0.5s), added mono_d2l topic, summary every 25 frames
# Used by: depth_comparison_test.launch.py → depth_scan_benchmark_node
# DO NOT EDIT — create a new versioned file (e.g., depth_comparison_benchmark_v3.yaml)
depth_scan_benchmark:
  ros__parameters:
    stereo_scan_topic: "/scan_stereo"
    mono_scan_topic: "/scan_mono"
    mono_d2l_scan_topic: "/scan_mono_d2l"
    output_dir: "/home/sidd/wheelchair_nav/eval_output/depth_comparison"
    sync_slop: 0.5
    fp_threshold_m: 0.5
    summary_interval: 25
