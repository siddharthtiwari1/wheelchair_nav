<!--
WHEELCHAIR KINOFLOW C++ NAVIGATION BEHAVIOR TREE
=================================================
Standard Nav2 BT with FollowPath action (C++ KinoFlow controller plugin).
Based on: wheelchair_robust_nav.xml

vs wheelchair_kinoflow_nav.xml (standalone Python node):
  - RESTORED FollowPath action (controller_server manages KinoFlow)
  - REMOVED GoalReached polling hack (GoalChecker handled internally)
  - Same backup-first recovery strategy

Pairs with: nav2_params_kinoflow_cpp.yaml, wheelchair_kinoflow_cpp_nav.launch.py

Created: 2026-02-28
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">

    <!-- Main navigation with 7 recovery attempts -->
    <RecoveryNode number_of_retries="7" name="NavigateRecovery">

      <!-- Primary navigation pipeline -->
      <PipelineSequence name="NavigateWithReplanning">

        <!-- PATH PLANNING - 1.5Hz replanning for dynamic environments -->
        <RateController hz="1.5">
          <RecoveryNode number_of_retries="1" name="ComputePathRecoveryNode">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"
                               error_code_id="{compute_path_error_code}"/>
            <ClearEntireCostmap name="ClearGlobalOnPlanFail"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>

        <!-- PATH FOLLOWING - KinoFlow C++ plugin via controller_server -->
        <RecoveryNode number_of_retries="1" name="FollowPathRecoveryNode">
          <FollowPath path="{path}" controller_id="FollowPath"
                      goal_checker_id="general_goal_checker"
                      error_code_id="{follow_path_error_code}"/>
          <ClearEntireCostmap name="ClearLocalOnFollowFail"
            service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>

      </PipelineSequence>

      <!-- RECOVERY ACTIONS - Backup-first wheelchair-optimized strategy -->
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>

        <RoundRobin name="RecoveryActions">
          <!-- 1. Backup first (most common stuck scenario) -->
          <BackUp backup_dist="0.15" backup_speed="0.05" time_allowance="3.0"/>

          <!-- 2. Clear costmaps (remove stale obstacles) -->
          <Sequence name="CostmapClearRecovery">
            <ClearEntireCostmap name="ClearGlobalCostmap"
              service_name="global_costmap/clear_entirely_global_costmap"/>
            <ClearEntireCostmap name="ClearLocalCostmap"
              service_name="local_costmap/clear_entirely_local_costmap"/>
          </Sequence>

          <!-- 3. Wait for dynamic obstacles to move -->
          <Wait wait_duration="2.0"/>

          <!-- 4. Small spin CW (30 deg) -->
          <Spin spin_dist="0.52" time_allowance="3.0"/>

          <!-- 5. Longer backup -->
          <BackUp backup_dist="0.20" backup_speed="0.05" time_allowance="4.0"/>

          <!-- 6. Small spin CCW (30 deg) -->
          <Spin spin_dist="-0.52" time_allowance="3.0"/>

          <!-- 7. Last resort - clear and aggressive backup -->
          <Sequence name="AggressiveRecovery">
            <ClearEntireCostmap name="ClearBeforeAggressiveBackup"
              service_name="local_costmap/clear_entirely_local_costmap"/>
            <BackUp backup_dist="0.25" backup_speed="0.05" time_allowance="5.0"/>
          </Sequence>
        </RoundRobin>
      </ReactiveFallback>

    </RecoveryNode>

  </BehaviorTree>
</root>
